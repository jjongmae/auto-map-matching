# Map Matcher UI Dependencies
PySide6
PyYAML
opencv-python

# VAID GIS (local package)
# Install with: pip install -e packages/vaid_gis
# Dependencies for vaid_gis:
numpy
geopandas
shapely
scipy

# Deep Learning / Segmentation
# Use --index-url https://download.pytorch.org/whl/cu118 (or appropriate CUDA version) if needed manually
torch
torchvision
transformers
pillow

# SAM3 지원 (8.3.237 이상 필요)
ultralytics>=8.3.237
